выводит на canvas текст
выглядит как старый терминал(зеленые буквы на черном фоне)

TO DO
добавить возможность сохранения в PNG(JPEG), GIF ? ( canvas --> png|jpeg|gif )
возможность работы с текстом пользователя (txt --> canvas )
понадобятся <img> и <input type="text">
